name: build

on:
  create:
    tags:
     - '*'

jobs:
  build:
    name: Building on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
         - os: ubuntu-latest
           kubectl_artifact_name: kubectl
           kubectl_asset_name: kubectl-linux-amd64
         - os: windows-latest
           kubectl_artifact_name: kubectl.exe
           kubectl_asset_name: kubectl-windows-amd64.exe
         - os: macos-latest
           kubectl_artifact_name: kubectl
           kubectl_asset_name: kubectl-macos-amd64
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.16

    - name: Make the build dir
      run: mkdir "${{ runner.workspace }}/build"
      
    - name: Build
      run: go build -o ${{ runner.workspace }}/build ./...

    - name: Upload zkubectl binaries to release
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file: ${{ runner.workspace }}/build/${{ matrix.kubectl_artifact_name }}
        asset_name: ${{ matrix.kubectl_asset_name }}
        tag: ${{ github.ref }}
